server {
    listen 80;
    server_name _;

    root /var/www/html/public;
    index index.php;

    charset utf-8;

    # Has to clear the 5 MB API limit with multipart overhead on top,
    # otherwise nginx rejects the upload before PHP can explain why.
    client_max_body_size 12m;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    location ~ \.php$ {
        # Docker's internal DNS, re-checked every 10 seconds. Without this
        # nginx resolves the upstream once at startup and keeps answering 502
        # after the php container is restarted with a new address.
        resolver 127.0.0.11 valid=10s ipv6=off;
        set $upstream app:9000;

        fastcgi_pass $upstream;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_read_timeout 120s;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
